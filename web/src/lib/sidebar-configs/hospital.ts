import { clinicSidebarConfig } from "./clinic";

// For now, Hospital and Clinic share the same sidebar config.
// We can expand this later with Wards, Beds, Inpatient etc.
export const hospitalSidebarConfig = clinicSidebarConfig;
