The Students search functionality is now implemented.
*   `db/student_queries.py` contains the `search_students` function.
*   `web_app.py` uses this function in the `student_list` route.
*   `students.html` includes the search form.

The user's initial request was "i want search functionality". I have implemented it for students as a starting point.

Do you want to extend search functionality to other modules (Courses, Payments, etc.) or is the current implementation for students sufficient for now?